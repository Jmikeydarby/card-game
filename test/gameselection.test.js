const expect = require('chai').expect;
const assert = require('chai').assert;
const fs = require('fs');
const GameSelection = require('../app/game_selection');

describe('GameSelection', () => {
	it('returns the full selection of games', () => {
		console.log(GameSelection());
		

	})
})